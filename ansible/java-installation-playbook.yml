---
- name: Update Ubuntu and Install Java
  hosts: appserver
  become: true
  tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: yes

    - name: Install Java ( OpenJDK 21 )
      ansible.builtin.apt:
        name: openjdk-21-jdk
        state: present